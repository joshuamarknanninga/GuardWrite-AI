function Settings() {
  return (
    <section className="card">
      <h1>Settings</h1>
      <p className="meta">Future-ready OpenAI integration will be configurable here.</p>
      <p>Current mode: Local formatter only. No API key required.</p>
    </section>
  );
}

export default Settings;
